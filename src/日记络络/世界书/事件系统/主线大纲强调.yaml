# :<%_ if (getvar('stat_data.世界.当前主线故事大纲') !== undefined && getvar('stat_data.世界.当前主线故事大纲') !== '无') { _%>
---
主线大纲强调:
  rule:
    - 优先遵守主线事件系统中的要求来进行故事创作与变量更新
    - <user>绝对不会事先知道上方的故事大纲，这些内容是为了指导你进行故事创作，所有剧情不应该提前被<user>知道或者透露，在故事发生之前没人知道会是这样
  status:
    当前主线故事大纲: # :{{get_message_variable:stat_data.世界.当前主线故事大纲}}
# :<%_ } _%>
