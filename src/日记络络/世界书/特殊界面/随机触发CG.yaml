# :<%_ if (getvar('stat_data.络络.亲密度') > 60) { _%>
---
随机触发CG:
  rule: 你必须思考`关键CG列表`document中哪一张CG最适合当下场景与情节（若无CG适用，则允许不使用CG只使用背景和立绘），并在本次<update>中输出以下内容以触发CG
  format: |-
    <update>
    _.set('世界.下次响应界面选择判断', 2);
    ...
    </update>
    <!--已将世界.下次响应界面选择判断更新为2，根据上文场景接下来的场景将使用特殊CG，我将采用CG文件：${`关键CG列表`document中存在的CG文件名}.jpg-->

# :<%_ } _%>
